---
# The device group in the host file
device_os: ios-rtr
provider: "{{ cli }}"

cli:
  host: "{{ inventory_hostname }}"
  username: "{{ 'cisco' }}"
  password: "{{ 'cisco' }}"
  transport: cli
  authorize: yes
